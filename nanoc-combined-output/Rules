#!/usr/bin/env ruby

compile '/stylesheets/' do
  # TODO: Combine item.children into a single item.
end

compile '*' do
end

route '/stylesheets/' do
  item.identifier + 'whole.css'
end

route '*' do
  item.identifier.chop + '.' + item[:extension]
end
